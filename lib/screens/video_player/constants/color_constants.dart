import 'package:flutter/material.dart';

/// Color constants for video player UI
class VideoPlayerColors {
  // Netflix brand colors
  static const netflixRed = Color(0xFFE50914);

  // Background colors
  static const darkBackground = Color(0xFF0F0F0F);
  static const darkerBackground = Color(0xFF1A1A1A);

  // Retro TV colors
  static const retroGreen = Color(0xFF00FF00);
  static const retroGreenDark = Color(0xFF003300);

  // Error/Alert colors
  static const errorRed = Color(0xFFEF4444);
}
