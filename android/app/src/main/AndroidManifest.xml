<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Keep CPU + WiFi alive while the native download service streams -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Android 13+: recording/download notifications (REC progress, "Saved",
         "Scheduled recording skipped") need the runtime grant; asked
         contextually on the first record/schedule, never at app start. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Scheduled recordings: exact alarms wake the recording service at the
         programme's start time. SCHEDULE_EXACT_ALARM covers Android 12 (where
         it is granted by default); USE_EXACT_ALARM auto-grants on 13+ (fine for
         sideload distribution — revisit if Play ever happens). The boot
         receiver re-arms alarms, which reboots and app updates silently clear. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Storage permissions for file picker (Android 12 and below) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <!-- Recording destination on Android 9 and below (Mi Box class hardware):
         MediaStore.Downloads doesn't exist pre-Q, so the engine writes a
         plain file into public Download/ — which needs this, legacy-only. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    
    <!-- The TV keyboard's mic key records in-app (SpeechRecognizer) so the
         listening state is drawn inside our own keyboard instead of the system
         handing us a full-screen voice activity. Asked contextually on the
         first mic press, never at app start. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Package visibility for the voice key: without this, both
         isRecognitionAvailable() and the service bind come back empty on API
         30+ even when a recognizer is installed (QUERY_ALL_PACKAGES above
         covers it today, but this keeps voice working if that broad
         permission is ever dropped). -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH" />
        </intent>
    </queries>

    <!-- TV compatibility flags (do not restrict phone installs) -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <!-- RECORD_AUDIO above IMPLIES a required microphone, which would exclude
         every TV panel and box without one. Voice is an optional extra here —
         the keyboard hides its mic key when there's no recognizer — so the
         requirement is explicitly waived. -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- Screen support declaration for proper TV scaling -->
    <!-- This prevents Android TV from applying compatibility scaling (zoom) -->
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true"
        android:requiresSmallestWidthDp="320" />

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:usesCleartextTraffic="true"
        android:largeHeap="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:banner="@drawable/banner_debrify">
        <!-- Force the Skia renderer instead of Impeller/Vulkan. Some Android TV
             SoCs (e.g. MediaTek/Amlogic in certain TCL sets) advertise Vulkan 1.1
             but ship broken drivers, causing a hard crash on launch. Skia is the
             stable path across our TV-first device range; iOS is unaffected. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <service
            android:name="background.downloader.DownloadWorkerService"
            android:exported="false"
            android:foregroundServiceType="dataSync|mediaPlayback" />
        <service
            android:name=".download.MediaStoreDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <service
            android:name=".recording.LiveRecordingService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <!-- exported=false is correct for both: android:exported only gates
             NON-system senders, and these receive only protected system
             broadcasts (BOOT_COMPLETED / MY_PACKAGE_REPLACED) and this app's
             own alarm PendingIntents. -->
        <receiver
            android:name=".recording.RecordingAlarmReceiver"
            android:exported="false" />
        <receiver
            android:name=".recording.RecordingBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <!-- Granting "Alarms & reminders" must upgrade existing
                     inexact schedules to exact ones. -->
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Android TV launcher entry -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <!-- Magnet link handler -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="magnet" />
            </intent-filter>
            <!-- Stremio addon handler -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="stremio" />
            </intent-filter>
            <!-- Debrify deep link handler (OAuth callbacks, etc.) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="debrify" />
            </intent-filter>
            <!-- HTTPS manifest.json handler for Stremio addons -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:pathPattern=".*manifest\\.json" />
            </intent-filter>
            <!-- URL share handler (appears in share sheet) -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- OAuth redirect is handled by flutter_appauth via manifest placeholders -->
        </activity>
        <activity
            android:name=".tv.TorboxTvPlayerActivity"
            android:exported="false"
            android:hardwareAccelerated="true"
            android:configChanges="keyboard|keyboardHidden|navigation|screenSize|screenLayout|smallestScreenSize|orientation|uiMode"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity
            android:name=".tv.AndroidTvTorrentPlayerActivity"
            android:exported="false"
            android:hardwareAccelerated="true"
            android:configChanges="keyboard|keyboardHidden|navigation|screenSize|screenLayout|smallestScreenSize|orientation|uiMode"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Allow querying for apps that can handle video URLs -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
            <data android:mimeType="video/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
            <data android:mimeType="video/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
            <data android:mimeType="application/octet-stream" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
            <data android:mimeType="application/octet-stream" />
        </intent>
        <!-- Allow querying for video player apps -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
