<?xml version="1.0" encoding="utf-8"?>
<!-- OTT guide: "The Shelf" — bottom row of landscape stills, the film keeps
     the top two thirds. Swapped into android_tv_playlist_overlay by
     installGuideOverlaySkin(); keeps the legacy id contract
     (android_tv_playlist + season_tabs_container). -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <View
        android:layout_width="match_parent"
        android:layout_height="360dp"
        android:layout_gravity="bottom"
        android:background="@drawable/gd_ott_scrim" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:paddingStart="48dp"
        android:paddingEnd="48dp"
        android:paddingBottom="20dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/gd_show_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:maxWidth="380dp"
                android:maxLines="1"
                android:ellipsize="end"
                android:textColor="#FFFFFFFF"
                android:textSize="17sp"
                android:fontFamily="sans-serif-medium" />

            <HorizontalScrollView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="20dp"
                android:scrollbars="none">
                <LinearLayout
                    android:id="@+id/season_tabs_container"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical" />
            </HorizontalScrollView>
        </LinearLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/android_tv_playlist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:clipToPadding="false"
            android:clipChildren="false"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:focusable="true"
            android:focusableInTouchMode="true" />

        <!-- Focused episode's synopsis; fixed two lines so the shelf never jumps. -->
        <TextView
            android:id="@+id/gd_caption"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:minLines="2"
            android:maxLines="2"
            android:ellipsize="end"
            android:textColor="#99FFFFFF"
            android:textSize="12sp" />
    </LinearLayout>
</FrameLayout>
