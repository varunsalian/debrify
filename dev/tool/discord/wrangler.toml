name = "debrify-discord"
main = "src/worker.js"
compatibility_date = "2025-07-01"
workers_dev = true

# Non-secret config. The bot token is NOT needed by the Worker (it replies via the
# interaction token). Only GITHUB_TOKEN is a real secret → `wrangler secret put GITHUB_TOKEN`.
[vars]
APP_ID = "1530971783706972200"
TRACKER_REPO = "varunsalian/debrify-tracker"
DISCORD_PUBLIC_KEY = "1d1eec985589f732b9d8adab166611c619dea4c2677a4a2f15dfb5bf07802c41"
